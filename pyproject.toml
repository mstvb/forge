[project]
name = "forge"
version = "1.0.0"
description = "Local Version Control"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
]

[project.scripts]
forge = 'forge.forge:cli'

[dependency-groups]
dev = [
    "ruff>=0.15.0",
    "ty>=0.0.14",
]
